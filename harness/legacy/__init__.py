"""harness.legacy — frozen v1 stack (curated SceneSDK genre pipeline).

The day-1 row-1 demos run on this: `sdk` (curated SceneSDK), the L0->L1->L2
`verifier` funnel, the `generator` + offline `templates`, and the greedy
`navigator`. Frozen — new work targets v2 (core/verify/gen). Kept green by the
legacy test suite.
"""
