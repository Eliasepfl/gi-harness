"""harness.core — version-spanning substrate and shared services.

The pieces every version of the harness depends on: the minimal physics
substrate (`world`), the execution sandbox (`sandbox`), the run-integrity
manifest (`integrity`), the telemetry ledger (`telemetry`), and the parts
bank loader/resolver (`bank`).
"""
